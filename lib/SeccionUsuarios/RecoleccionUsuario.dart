import 'package:flutter/material.dart';

class RecoleccionUsuario extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Recoleccion de Usuario'),
      ),
    );
  }
}
